#include <stdio.h>

/**
 * Descrption: main - entry point
 * write your lin of code here....
 *Remember:
 * - you are not allowed to use a
 * - you are not allowed to modify p
 * - only one staement
 * - you ard not allowed to code anythng else than this line of code
 */
int main(Add one line to this code, so that the program prints a[2] = 98)
{
	int n;
	int a[5];
	int *p;

	a[2] =
	*(p + 5) = 98;
	p = &n;
	/* ...so that this prints 98\n */
	printf("a[2] = %d\n", a[2]);
	return (0);
}
